function out = GenRand2D(nsample)
out = randn(nsample, 2);
maxv = max(out);
angle = rand(1,1)*10*pi;
S = [rand(1,1)*10 rand(1,1)*20];
R = [cos(angle) -sin(angle);sin(angle) cos(angle)];
T = ones(nsample, 1)*[rand(1,1)*maxv(1) rand(1,1)*maxv(2)];
out = out * 
out = out * R;
out = out - T;
